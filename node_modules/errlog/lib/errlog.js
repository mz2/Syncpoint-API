exports.e = function(fun, args) {
    return function(err) {
        if (err) {
            console.error("ERROR: ", err);
            if (err.stack) {
                console.error(err.stack);
            }
        } else {
            fun && fun.apply(this, args || arguments)
        }
    };
};
